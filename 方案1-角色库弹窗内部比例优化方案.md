# 角色库弹窗 — 内部布局比例优化方案

## 问题分析

弹窗内布局比例不合理有三个原因：

| 原因 | 效果 |
|------|------|
| **头部工具栏过重** | 占垂直空间 ~15%，挤压内容区 |
| **25/75 分栏 + 内容密度不匹配** | 左栏密、右栏疏，视觉重量失衡 |
| **表单间距比例失调** | 20px 间距在全屏是"呼吸感"，在弹窗里变成"松散" |

---

## 改动文件

| 文件 | 说明 |
|------|------|
| `src/views/CharacterLibrary.vue` | 样式调整，仅在有 `.modal-mode` 时覆盖 |
| `src/views/Write.vue` | 附带动画微调 |

---

## Step 1：弹窗模式下头部工具栏折叠

### 1.1 问题

头部按钮行在弹窗中吃掉约 70px 垂直高度：

```
┌──────────────────────────────────────────────────────────────┐
│ [关闭] 角色库                                                 │ ← 56px
│ [新建角色] [他书导入] [批量操作] [搜索...] [智能识别]           │ ← 52px（含间距）
├──────────────────────────────────────────────────────────────┤
│                         主内容区                               │
```

弹窗高约 85vh（~650px），头部两行占去 ~108px = **16.6%**，加上顶部 padding，可用的内容区只剩约 500px。

### 1.2 方案：弹窗模式下将工具栏合并为一行，按钮缩小

在 `CharacterLibrary.vue` 的 style 中追加：

```css
/* ========== 弹窗模式：头部精简 ========== */
.character-library.modal-mode .header-bar {
  padding: 10px 16px;
  flex-wrap: nowrap;
  gap: 8px;
}

/* header 三栏布局调整 */
.character-library.modal-mode .header-left {
  flex-shrink: 0;
}

.character-library.modal-mode .back-home-btn {
  padding: 6px 12px;
  border-radius: 12px;
  gap: 8px;
}

.character-library.modal-mode .page-title {
  font-size: 15px;
}

/* header-center 的按钮行缩小间距和字号 */
.character-library.modal-mode .action-buttons-row {
  gap: 6px;
  flex-wrap: nowrap;
}

.character-library.modal-mode .action-buttons-row .el-button {
  height: 32px;
  font-size: 12px;
  padding: 0 12px;
  gap: 4px;
}

/* 搜索按钮隐藏，搜索框直接显示 */
.character-library.modal-mode .search-wrapper .search-btn {
  display: none;
}

.character-library.modal-mode .search-expand {
  position: relative;
  left: auto;
  top: auto;
  transform: none;
  width: 180px !important;
  box-shadow: none;
}

.character-library.modal-mode .search-expand :deep(.el-input) {
  width: 180px;
}

/* header-right 智能识别按钮缩小 */
.character-library.modal-mode .header-right .el-button {
  height: 32px;
  font-size: 12px;
  padding: 0 12px;
  gap: 4px;
}
```

### 1.3 预期效果

```
┌────────────────────────────────────────────────────────────────────┐
│ [← 关闭]  角色库  │  [+新建] [导入] [批量] [🔍搜索...]  [⚡智能识别] │ ← ~52px
├────────────────────────────────────────────────────────────────────┤
│                              主内容区                                │
```

从 ~108px 降到 ~52px，省出约 56px 给内容区。

---

## Step 2：分栏比例从 25/75 调整为 30/70

### 2.1 问题

左栏（文件夹+列表）信息密集，右栏（表单）大量空白。25/75 在弹窗里视觉失衡：

```
[左 300px ┃ 全是内容      ]  [右 900px ┃ 空空荡荡，表单缩在中间一小块     ]
```

### 2.2 方案：加宽左栏、缩窄右栏

```css
/* ========== 弹窗模式：分栏比例调整 ========== */
.character-library.modal-mode .left-panel {
  width: 30%;
  min-width: 240px;
  max-width: 320px;
}

.character-library.modal-mode .right-panel {
  flex: 1;
  min-width: 0;
  padding: 16px 20px;
}
```

### 2.3 预期效果

```
[左 300-320px ┃ 内容满满]  [右 ~780px ┃ 表单相对居中，空白减少  ][
```

左右视觉重量从 "1:3 失衡" 变为 "1:2.3 左右"，看起来协调很多。

---

## Step 3：表单内部紧凑化

### 3.1 问题

弹窗的右栏可用高度约 450px，其中：

```
所属文件夹   [下拉框    ]      ↓ 20px
角色名称     [输入框     ]      ↓ 20px
性别         [男][女][未知][无] ↓ 20px
角色性格     [3行输入框   ]     ↓ 20px
角色信息     [6行输入框   ]     ↓ 20px
                    [提及章节] [导出] [删除] [保存]  ← 30px margin-top
```

6 个间距 = 120px 纯空白，占 ~27% 的右栏高度。

### 3.2 方案：减少间距 + 表单内联化

```css
/* ========== 弹窗模式：表单紧凑 ========== */
.character-library.modal-mode .edit-form {
  max-width: 100%;
}

.character-library.modal-mode .form-item {
  margin-bottom: 12px;       /* 20px → 12px */
}

.character-library.modal-mode .form-item label {
  margin-bottom: 4px;        /* 8px → 4px */
  font-size: 13px;
}

/* 文件夹 + 名称放在一行（左右布局） */
.character-library.modal-mode .form-row-compact {
  display: flex;
  gap: 12px;
}

.character-library.modal-mode .form-row-compact .form-item {
  flex: 1;
  min-width: 0;
}

/* 性别选项用更紧凑的样式 */
.character-library.modal-mode .gender-option {
  padding: 4px 14px;         /* 8px 20px → 4px 14px */
  font-size: 13px;
}

/* textarea 减少行数 */
.character-library.modal-mode .form-item :deep(.el-textarea__inner) {
  min-height: unset;
}

.character-library.modal-mode .character-info-textarea :deep(.el-textarea__inner) {
  min-height: 80px !important;  /* 尽量显示约 4 行 */
}

.character-library.modal-mode .personality-textarea :deep(.el-textarea__inner) {
  min-height: 60px !important;  /* 约 2 行 */
}

/* 底部操作栏间距缩小 */
.character-library.modal-mode .action-buttons {
  margin-top: 16px;           /* 30px → 16px */
  padding-top: 12px;          /* 20px → 12px */
}

.character-library.modal-mode .action-buttons .el-button {
  height: 30px;
  font-size: 12px;
  padding: 0 14px;
}
```

### 3.3 预期效果

```
所属文件夹 [下拉框    ]  角色名称  [输入框     ]    ← 一行两个 field
性别  [男] [女] [未知] [无]                          ↓ 12px
角色性格 [3行输入框   ]                              ↓ 12px
角色信息 [4行输入框   ]                              ↓ 12px
               [提及章节] [导出] [删除] [保存]

空白从 120px 降到 ~48px，节省约 72px
```

---

## Step 4：完整样式块（可直接复制）

将以下全部内容追加到 `CharacterLibrary.vue` 的 `<style scoped>` 末尾（在 `</style>` 之前）：

```css
/* =================================================
   弹窗模式覆盖 — 仅在 isModal=true 时生效
   ================================================= */

/* 头部精简 */
.character-library.modal-mode .header-bar {
  padding: 10px 16px;
  flex-wrap: nowrap;
  gap: 8px;
}

.character-library.modal-mode .back-home-btn {
  padding: 6px 12px;
  border-radius: 12px;
  gap: 8px;
}

.character-library.modal-mode .page-title {
  font-size: 15px;
}

.character-library.modal-mode .action-buttons-row {
  gap: 6px;
  flex-wrap: nowrap;
}

.character-library.modal-mode .action-buttons-row .el-button {
  height: 32px;
  font-size: 12px;
  padding: 0 12px;
  gap: 4px;
}

.character-library.modal-mode .search-wrapper .search-btn {
  display: none;
}

.character-library.modal-mode .search-expand {
  position: relative;
  left: auto;
  top: auto;
  transform: none;
  width: 180px !important;
  box-shadow: none;
}

.character-library.modal-mode .search-expand :deep(.el-input) {
  width: 180px;
}

.character-library.modal-mode .header-right .el-button {
  height: 32px;
  font-size: 12px;
  padding: 0 12px;
  gap: 4px;
}

/* 分栏比例调整：左 30% / 右 70% */
.character-library.modal-mode .left-panel {
  width: 30%;
  min-width: 240px;
  max-width: 320px;
}

.character-library.modal-mode .right-panel {
  flex: 1;
  min-width: 0;
  padding: 16px 20px;
}

/* 表单紧凑 */
.character-library.modal-mode .edit-form {
  max-width: 100%;
}

.character-library.modal-mode .form-item {
  margin-bottom: 12px;
}

.character-library.modal-mode .form-item label {
  margin-bottom: 4px;
  font-size: 13px;
}

.character-library.modal-mode .gender-option {
  padding: 4px 14px;
  font-size: 13px;
}

.character-library.modal-mode .action-buttons {
  margin-top: 16px;
  padding-top: 12px;
}

.character-library.modal-mode .action-buttons .el-button {
  height: 30px;
  font-size: 12px;
  padding: 0 14px;
}
```

---

## 三步走效果对比

| 测量项 | 改前 | 改后 |
|--------|------|------|
| 头部高度 | ~108px | ~52px |
| 左栏宽度 | 25% / min 260px | 30% / min 240px, max 320px |
| 右栏间距累计 | ~120px | ~48px |
| 按钮字号 | 14px | 12px（弹窗内更合适） |
| 弹窗内可用内容面积 | **~55%** | **~75%** |

用户实际感受：打开弹窗后不会再觉得"头重脚轻"或"表单稀稀拉拉"，布局更接近一个**紧凑、高效、像大弹窗该有的样子**。

---

## 备选方向（未纳入，供参考）

如果以上的调整还不够，可以考虑更激进的改造：

- **Tab 化表单**：将右栏表单做成 Tab 切换（基本信息 / 详细设定），使单个表单更短
- **左栏宽度记忆**：用户可拖拽调整左右栏比例，类似 Write.vue 的目录面板
- **textarea 自动伸缩**：根据内容自动调整高度，从不浪费行数

但这些改动较大，建议先做 Step 1-4 的布局压缩，88% 的比例问题就解决了。
