# 角色库弹窗 — 按钮暗色主题适配（5 个方案）

## 问题定位

弹窗内的按钮在暗色模式下没有正确适配，具体缺失的暗色样式包括：

| 位置 | 元素 | 缺失表现 |
|------|------|----------|
| 顶部工具栏 | `.action-buttons-row .el-button`（新建角色/导入/批量） | 白底白字或亮色 |
| 顶部右侧 | `.header-right .el-button`（智能识别） | 主题色按钮无暗色适配 |
| 性别选择 | `.gender-option` | 亮绿渐变背景无暗色变体 |
| 底部操作栏 | `.action-buttons .el-button`（保存/删除等） | 按钮背景和文字未切换 |
| 新增文件夹 | `.add-folder-btn` | 亮色背景 |
| 弹窗右上角 | `.modal-close-btn`（X按钮） | 已适配（Write.vue 已有） |

---

## 方案 1：CSS 变量注入 — Element Plus 原生暗色（最推荐）

### 原理

Element Plus 所有组件都通过 CSS 变量（`--el-color-*`）控制颜色。在弹窗遮罩容器上覆盖这些变量，所有 el-button、el-input、el-dialog 等自动跟随。

### 改动位置

`src/views/Write.vue` — 在 `.character-library-overlay` 的暗色样式中追加 CSS 变量

### 改动内容

```diff
 :root[data-theme='dark'] .character-library-overlay {
   background: rgba(0, 0, 0, 0.6);
+  /* Element Plus 组件变量覆盖 — 让弹窗内的 el-button 等使用暗色 */
+  --el-bg-color: #1e293b;
+  --el-bg-color-overlay: #1e293b;
+  --el-text-color-primary: #e5e7eb;
+  --el-text-color-regular: #d1d5db;
+  --el-border-color: #334155;
+  --el-border-color-light: #334155;
+  --el-fill-color: #334155;
+  --el-fill-color-light: #2d3a4e;
+  --el-fill-color-lighter: #273445;
+  --el-color-white: #1e293b;
+  --el-color-black: #f3f4f6;
+  --el-color-info-light-9: #273445;
+  --el-color-primary: #5eead4;
+  --el-color-primary-light-3: #2dd4bf;
+  --el-color-danger: #f87171;
+  --el-color-danger-light-3: #ef4444;
+  --el-color-warning: #fbbf24;
+  --el-color-success: #34d399;
+  --el-button-bg-color: #334155;
+  --el-button-border-color: #475569;
+  --el-button-hover-bg-color: #475569;
+  --el-button-hover-border-color: #5eead4;
+  --el-button-hover-text-color: #5eead4;
+  --el-button-text-color: #e5e7eb;
+  --el-button-active-bg-color: #2d3a4e;
 }
```

### 效果

所有 Element Plus 按钮（el-button）、输入框（el-input）、下拉框（el-select）、对话框（el-dialog）在弹窗内自动切换到暗色。零额外 CSS 选择器。

### 优缺点

| 优点 | 缺点 |
|------|------|
| 改动最少（只加 CSS 变量） | 需要知道哪些变量需要覆盖 |
| 覆盖所有 el-* 组件 | 变量名较多（约 20 行） |
| 不需要动 CharacterLibrary.vue | 变量和项目主题色需要保持一致 |

---

## 方案 2：CharacterLibrary.vue 补全弹窗模式暗色样式

### 原理

在 CharacterLibrary.vue 的 `<style scoped>` 中，用 `:root[data-theme='dark'] .character-library.modal-mode` 选择器为弹窗模式下所有元素补齐暗色样式。

### 改动位置

`src/views/CharacterLibrary.vue` — style 末尾追加

### 改动内容

```css
/* ========== 弹窗模式暗色按钮适配 ========== */
:root[data-theme='dark'] .character-library.modal-mode .action-buttons-row .el-button {
  --el-button-bg-color: #334155;
  --el-button-border-color: #475569;
  --el-button-text-color: #e5e7eb;
  --el-button-hover-bg-color: #475569;
  --el-button-hover-border-color: #5eead4;
  --el-button-hover-text-color: #5eead4;
}

:root[data-theme='dark'] .character-library.modal-mode .header-right .el-button--primary {
  --el-button-bg-color: #0d9488;
  --el-button-border-color: #14b8a6;
  --el-button-text-color: #fff;
  --el-button-hover-bg-color: #0f766e;
  --el-button-hover-border-color: #0d9488;
}

:root[data-theme='dark'] .character-library.modal-mode .gender-option {
  background: linear-gradient(135deg, #2d3a4e 0%, #334155 100%);
  color: #d1d5db;
}
:root[data-theme='dark'] .character-library.modal-mode .gender-option:hover {
  background: linear-gradient(135deg, #334155 0%, #475569 100%);
  transform: translateY(-2px);
}
:root[data-theme='dark'] .character-library.modal-mode .gender-option.active {
  background: linear-gradient(135deg, #0d9488 0%, #14b8a6 100%);
  color: #fff;
}

:root[data-theme='dark'] .character-library.modal-mode .add-folder-btn {
  background: linear-gradient(135deg, #2d3a4e 0%, #334155 100%);
  color: #94a3b8;
}
:root[data-theme='dark'] .character-library.modal-mode .add-folder-btn:hover {
  background: linear-gradient(135deg, #0d9488 0%, #14b8a6 100%);
  color: #fff;
}

:root[data-theme='dark'] .character-library.modal-mode .action-buttons .el-button--primary {
  --el-button-bg-color: #0d9488;
  --el-button-border-color: #14b8a6;
  --el-button-text-color: #fff;
  --el-button-hover-bg-color: #0f766e;
  --el-button-hover-border-color: #0d9488;
}

:root[data-theme='dark'] .character-library.modal-mode .action-buttons .el-button--danger {
  --el-button-bg-color: #7f1d1d;
  --el-button-border-color: #991b1b;
  --el-button-text-color: #fca5a5;
  --el-button-hover-bg-color: #991b1b;
  --el-button-hover-border-color: #dc2626;
  --el-button-hover-text-color: #fef2f2;
}

:root[data-theme='dark'] .character-library.modal-mode .action-buttons .el-button:not(.el-button--primary):not(.el-button--danger) {
  --el-button-bg-color: #334155;
  --el-button-border-color: #475569;
  --el-button-text-color: #d1d5db;
  --el-button-hover-bg-color: #475569;
  --el-button-hover-border-color: #5eead4;
  --el-button-hover-text-color: #5eead4;
}
```

### 优缺点

| 优点 | 缺点 |
|------|------|
| 精确控制每个按钮样式 | 需要写大量 CSS |
| 影响范围局限在弹窗内 | 每新增一个未覆盖元素都可能漏掉 |
| 支持自定义渐变等复杂样式 | 和方案 1 重复劳动 |

---

## 方案 3：全局 unscoped 样式 + `:deep()` 穿透

### 原理

在 Write.vue 中用 **unscoped `<style>` 块**（不加 scoped）或 `:deep()` 选择器，直接覆盖 Teleport 到 body 下的弹窗内按钮样式。利用 CSS 优先级覆盖 Element Plus 的默认样式。

### 改动位置

`src/views/Write.vue` — 新增一个 `<style>` 块（不加 scoped）

### 改动内容

在 Write.vue 现有的 `<style scoped>` 之后（`</style>` 之后），追加一个全局样式块：

```vue
<!-- 追加在现有 </style> 之后 -->
<style>
/* 角色库弹窗 — Element Plus 暗色覆盖（全局） */
:root[data-theme='dark'] .character-library-overlay .modal-container {
  --el-bg-color: #1e293b;
  --el-bg-color-overlay: #1e293b;
  --el-text-color-primary: #e5e7eb;
  --el-text-color-regular: #d1d5db;
  --el-border-color: #334155;
  --el-border-color-light: #334155;
  --el-fill-color: #334155;
  --el-fill-color-light: #2d3a4e;
  --el-fill-color-lighter: #273445;
}

:root[data-theme='dark'] .character-library-overlay .el-button {
  --el-button-bg-color: #334155;
  --el-button-border-color: #475569;
  --el-button-text-color: #e5e7eb;
}
:root[data-theme='dark'] .character-library-overlay .el-button:hover {
  --el-button-bg-color: #475569;
  --el-button-border-color: #5eead4;
  --el-button-text-color: #5eead4;
}
:root[data-theme='dark'] .character-library-overlay .el-button--primary {
  --el-button-bg-color: #0d9488;
  --el-button-border-color: #14b8a6;
  --el-button-text-color: #fff;
}
:root[data-theme='dark'] .character-library-overlay .el-button--primary:hover {
  --el-button-bg-color: #0f766e;
  --el-button-border-color: #0d9488;
}
:root[data-theme='dark'] .character-library-overlay .el-button--danger {
  --el-button-bg-color: #7f1d1d;
  --el-button-border-color: #991b1b;
  --el-button-text-color: #fca5a5;
}
:root[data-theme='dark'] .character-library-overlay .el-button--danger:hover {
  --el-button-bg-color: #991b1b;
  --el-button-border-color: #dc2626;
  --el-button-text-color: #fef2f2;
}

/* 性别选项等非 el- 元素 */
:root[data-theme='dark'] .character-library-overlay .gender-option {
  background: linear-gradient(135deg, #2d3a4e 0%, #334155 100%);
  color: #d1d5db;
}
:root[data-theme='dark'] .character-library-overlay .gender-option:hover {
  background: linear-gradient(135deg, #334155 0%, #475569 100%);
}
:root[data-theme='dark'] .character-library-overlay .gender-option.active {
  background: linear-gradient(135deg, #0d9488 0%, #14b8a6 100%);
  color: #fff;
}

:root[data-theme='dark'] .character-library-overlay .add-folder-btn {
  background: linear-gradient(135deg, #2d3a4e 0%, #334155 100%);
  color: #94a3b8;
}
:root[data-theme='dark'] .character-library-overlay .add-folder-btn:hover {
  background: linear-gradient(135deg, #0d9488 0%, #14b8a6 100%);
  color: #fff;
}
</style>
```

### 优缺点

| 优点 | 缺点 |
|------|------|
| 不受 scoped 限制，任意穿透 | 全局样式可能泄漏到其他页面 |
| 和方案 1 不冲突，可叠加使用 | 需要加 `.character-library-overlay` 前缀避免泄漏 |
| 管理方便（全在 Write.vue 里） | 和 CharacterLibrary.vue 自身的暗色样式职责不清 |

---

## 方案 4：Teleport 目标改为带主题 context 的容器

### 原理

当前 Teleport 到 `body`，但 `body` 不带有 Element Plus 的暗色上下文。改为 Teleport 到一个**在 Write.vue 内部、且受主题控制的容器**，这样 Element Plus 的暗色变量和 $theme 上下文自动继承。

### 改动位置

`src/views/Write.vue` — 修改 template 中的 Teleport 目标

### 改动内容

```diff
+<!-- 先在 template 中加一个挂载点 -->
+<div ref="modalMountPoint" class="modal-mount-point"></div>
+
 <!-- Teleport 目标从 body 改为 Write.vue 内部的挂载点 -->
 <Teleport :to="modalMountPoint">
   ...
 </Teleport>
```

或者更简单的 — 直接去掉 Teleport，用 fixed 定位在原地渲染：

```diff
-<Teleport to="body">
-  <Transition name="modal-scale">
-    <div v-if="showCharacterLibrary" class="character-library-overlay">
-      ...
-    </div>
-  </Transition>
-</Teleport>
+<!-- 不用 Teleport，直接在 Write.vue 的 template 根下渲染 -->
+<Transition name="modal-scale">
+  <div v-if="showCharacterLibrary" class="character-library-overlay">
+    <button class="modal-close-btn" @click="closeCharacterLibrary">
+      <el-icon><Close /></el-icon>
+    </button>
+    <div class="modal-container">
+      <CharacterLibraryModal
+        :book-id="bookId"
+        :is-modal="true"
+        @close="closeCharacterLibrary"
+        @select-character="handleCharacterSelected"
+      />
+    </div>
+  </div>
+</Transition>
```

这样 CharacterLibraryModal 的组件树在 Write.vue 的 DOM 内，scoped 样式、Element Plus 主题、CSS 变量全部自然继承。

> ⚠️ 注意：去掉 Teleport 后需要确保遮罩的 `position: fixed; z-index` 仍然有效（Write.vue 本身有 CSS stacking context，但 fixed 相对于视口定位，所以没问题。唯一要注意的是 Write.vue 的父容器有没有设置 `transform` 或 `filter` 导致新的 stacking context — 如果有，需要把遮罩放到根层级）。

### 优缺点

| 优点 | 缺点 |
|------|------|
| 主题继承自然，零额外适配工作 | 可能需要解决 stacking context 问题 |
| scoped 样式正常工作 | 遮罩的 z-index 需要保证盖过 Write.vue 全部内容 |
| 所有弹窗都能享受同一机制 | 对 Write.vue 的模板结构有一定要求 |

---

## 方案 5：JS 动态主题注入（Pinia/CSS Variables）

### 原理

在 Pinia store 中维护一个主题变量数组，弹窗打开时通过 JS 动态注入 `<style>` 标签到 `body` 末尾，关闭时移除。或者直接监听 `data-theme` 变化，通过 `document.documentElement` 的 CSS 自定义属性传递到弹窗内。

### 改动位置

- `src/stores/theme.ts`（新建或扩展已有 store）
- `src/views/Write.vue` — 在 `openCharacterLibrary` 中触发注入
- 或者 `src/composables/useCharacterLibrary.ts` — 在 composable 中处理

### 改动内容

**方式 A：Pinia 主题 Store**

```ts
// src/stores/theme.ts
import { defineStore } from 'pinia'

export const useThemeStore = defineStore('theme', {
  state: () => ({
    isDark: document.documentElement.getAttribute('data-theme') === 'dark',
  }),
  actions: {
    getModalCSSVariables() {
      return this.isDark ? {
        '--modal-bg': '#1e293b',
        '--modal-text': '#e5e7eb',
        '--modal-border': '#334155',
        '--btn-bg': '#334155',
        '--btn-border': '#475569',
        '--btn-text': '#e5e7eb',
        '--btn-primary-bg': '#0d9488',
        '--btn-primary-border': '#14b8a6',
        '--btn-danger-bg': '#7f1d1d',
        '--btn-danger-border': '#991b1b',
        '--btn-danger-text': '#fca5a5',
      } : {}
    }
  }
})
```

**方式 B：composable 中动态注入 style 标签**

```ts
// 在 useCharacterLibrary.ts 中
const injectDarkStyles = () => {
  const isDark = document.documentElement.getAttribute('data-theme') === 'dark'
  if (!isDark) return
  
  const style = document.createElement('style')
  style.id = 'character-library-dark-theme'
  style.textContent = `
    .character-library-overlay .el-button {
      --el-button-bg-color: #334155 !important;
      --el-button-border-color: #475569 !important;
      --el-button-text-color: #e5e7eb !important;
    }
    .character-library-overlay .el-button:hover {
      --el-button-bg-color: #475569 !important;
      --el-button-border-color: #5eead4 !important;
      --el-button-text-color: #5eead4 !important;
    }
    .character-library-overlay .el-button--primary {
      --el-button-bg-color: #0d9488 !important;
      --el-button-border-color: #14b8a6 !important;
      --el-button-text-color: #fff !important;
    }
    .character-library-overlay .gender-option {
      background: linear-gradient(135deg, #2d3a4e, #334155) !important;
      color: #d1d5db !important;
    }
    .character-library-overlay .gender-option.active {
      background: linear-gradient(135deg, #0d9488, #14b8a6) !important;
      color: #fff !important;
    }
  `
  document.head.appendChild(style)
}

const removeDarkStyles = () => {
  const style = document.getElementById('character-library-dark-theme')
  if (style) style.remove()
}

// 在 open 时注入，close 时移除
```

### 优缺点

| 优点 | 缺点 |
|------|------|
| 零 CSS 文件修改 | 侵入性强，JS 管理样式不易维护 |
| 动态按需加载，不污染全局 | 需要处理注入/移除时机 |
| 适合需要运行时主题切换的场景 | 不如纯 CSS 方案可靠 |

---

## 方案对比

| 方案 | 改动量 | 可靠性 | 维护性 | 扩散性 | 推荐场景 |
|------|--------|--------|--------|--------|----------|
| **① CSS 变量注入** | ~20 行（变量定义） | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 覆盖所有 el-* | ✅ **最推荐** |
| ② 组件内补全样式 | ~80 行 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 仅覆盖已写元素 | 需要精细控制 |
| ③ 全局 unscoped 样式 | ~50 行 | ⭐⭐⭐⭐ | ⭐⭐⭐ | 需加前缀防泄漏 | 不用动组件文件 |
| ④ 去 Teleport | 少但涉及结构 | ⭐⭐⭐ | ⭐⭐⭐⭐ | 零额外样式 | 项目重构时 |
| ⑤ JS 动态注入 | ~60 行 JS | ⭐⭐ | ⭐⭐ | 全部 JS 可控 | 运行时主题需求 |

---

### 我的建议

**选方案 ①**，只用改 Write.vue 一个文件，在已有的 `:root[data-theme='dark'] .character-library-overlay` 中追加约 20 行 CSS 变量，CharacterLibrary.vue 和路由都不动。Element Plus 的所有按钮、输入框、下拉框自动适配暗色，一劳永逸。

如果有些自定义元素（如 `.gender-option`）没有适配，再在方案①的基础上追加 5-10 行针对性的暗色样式即可，相当于方案①+②的组合，但总量仍然最少。
